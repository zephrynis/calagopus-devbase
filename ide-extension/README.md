# Calaforge devtools

In-IDE controls for a Calagopus extension dev environment: start/stop/restart
the panel backend and Vite frontend (pidfile-managed, safe next to manual
`cargo run`), toggle the environment's Wings node, run migrations, tail logs,
scaffold and package extensions, and show the seeded admin login.
